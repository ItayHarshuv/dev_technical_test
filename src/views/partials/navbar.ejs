<nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid navbar-container px-4 position-relative">
        <a class="navbar-brand d-flex align-items-center gap-2 ps-0 ms-0" href="/">
            <img src="/brand/icon.png" alt="Logo" class="navbar-icon">
            EMERALD STAY
        </a>
        <button class="navbar-toggler pe-0 me-0 border-0 d-flex d-lg-none align-items-center justify-content-center" type="button" id="navbarToggleBtn" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-nav d-none d-lg-flex" id="navbarNav">
            <a class="nav-link <%= typeof activePage !== 'undefined' && activePage === 'home' ? 'active' : '' %>" href="/">Calculator</a>
            <a class="nav-link <%= typeof activePage !== 'undefined' && activePage === 'admin' ? 'active' : '' %>" href="/admin">Admin</a>
        </div>
    </div>
</nav>

<!-- Full-screen overlay menu -->
<div class="position-fixed top-0 start-0 w-100 h-100 d-flex align-items-center justify-content-center fullscreen-menu-overlay" id="fullscreenMenu">
    <div class="fullscreen-menu-content w-90 d-flex flex-column overflow-auto">
        <div class="d-flex justify-content-between align-items-center mb-auto">
            <a class="navbar-brand text-primary text-uppercase d-flex align-items-center gap-2 text-decoration-none" href="/">
                <img src="/brand/icon.png" alt="Logo" class="navbar-icon">
                EMERALD STAY
            </a>
            <button class="btn btn-link p-0 fullscreen-menu-close d-flex align-items-center justify-content-center" id="closeMenuBtn" aria-label="Close menu">
                <span>&times;</span>
            </button>
        </div>
        <ul class="navbar-nav list-unstyled p-0 m-0 d-flex flex-column align-items-center justify-content-center flex-fill">
            <li class="w-100 text-center">
                <a class="nav-link fullscreen-nav-link d-block py-3 text-primary text-uppercase text-decoration-none <%= typeof activePage !== 'undefined' && activePage === 'home' ? 'active' : '' %>" href="/">Calculator</a>
            </li>
            <li class="w-100 text-center">
                <a class="nav-link fullscreen-nav-link d-block py-3 text-primary text-uppercase text-decoration-none <%= typeof activePage !== 'undefined' && activePage === 'admin' ? 'active' : '' %>" href="/admin">Admin</a>
            </li>
        </ul>
    </div>
</div>

<script src="/js/navbar.js"></script>

